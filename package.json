{
  "name": "@blackroad/inference-blackroadio",
  "version": "1.0.0",
  "scripts": {
    "dev": "npx serve .",
    "build": "echo 'Static site - no build needed'",
    "deploy": "wrangler pages deploy . --project-name=inference-blackroadio"
  },
  "devDependencies": {
    "wrangler": "^4.60.0"
  }
}
